name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  portal-go:
    runs-on: ubuntu-latest
    name: "portal: go"
    defaults:
      run:
        working-directory: web-portal
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.27.x"
      - name: Fmt
        run: |
          set -euo pipefail
          files="$(gofmt -l .)"
          if [ -n "$files" ]; then
            printf '%s\n' "$files"
            exit 1
          fi
      - name: Vet
        run: go vet ./...
      - name: Test
        run: go test ./...
      - name: Race
        run: go test -race ./...
      - name: Install gosec
        run: go install github.com/securego/gosec/v2/cmd/gosec@v2.29.0
      - name: Gosec
        run: |
          set -euo pipefail
          export PATH="$(go env GOPATH)/bin:$PATH"
          gosec -fmt=text ./...
      - name: Build
        run: make build-offline

  updater-go:
    runs-on: ubuntu-latest
    name: "updater: go"
    defaults:
      run:
        working-directory: updater
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.27.x"
      - name: Fmt
        run: |
          set -euo pipefail
          files="$(gofmt -l .)"
          if [ -n "$files" ]; then
            printf '%s\n' "$files"
            exit 1
          fi
      - name: Vet
        run: go vet ./...
      - name: Test
        run: go test ./...
      - name: Race
        run: go test -race ./...
      - name: Install gosec
        run: go install github.com/securego/gosec/v2/cmd/gosec@v2.29.0
      - name: Gosec
        run: |
          set -euo pipefail
          export PATH="$(go env GOPATH)/bin:$PATH"
          gosec -fmt=text ./...
      - name: Build
        run: go build ./...

  backup-go:
    runs-on: ubuntu-latest
    name: "backup: go"
    defaults:
      run:
        working-directory: backup
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.27.x"
      - name: Fmt
        run: |
          set -euo pipefail
          files="$(gofmt -l .)"
          if [ -n "$files" ]; then
            printf '%s\n' "$files"
            exit 1
          fi
      - name: Vet
        run: go vet ./...
      - name: Test
        run: go test ./...
      - name: Race
        run: go test -race ./...
      - name: Install gosec
        run: go install github.com/securego/gosec/v2/cmd/gosec@v2.29.0
      - name: Gosec
        run: |
          set -euo pipefail
          export PATH="$(go env GOPATH)/bin:$PATH"
          gosec -fmt=text ./...
      - name: Build
        run: go build ./...

  shell:
    runs-on: ubuntu-latest
    name: "lint: shell"
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.27.x"
      - name: Install shellcheck
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y shellcheck
      - name: Install shfmt
        run: go install mvdan.cc/sh/v3/cmd/shfmt@v3.14.1
      - name: Shellcheck
        run: shellcheck -S warning scripts/*.sh scripts/lib/*.sh web-proxy/render-template.sh
      - name: Shfmt
        run: |
          set -euo pipefail
          export PATH="$(go env GOPATH)/bin:$PATH"
          shfmt -d scripts/*.sh scripts/lib/*.sh web-proxy/render-template.sh

  lua:
    runs-on: ubuntu-latest
    name: "lint: lua"
    defaults:
      run:
        working-directory: server/snikket-modules
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Install lua and luarocks
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y lua5.1 luarocks
      - name: Install luacheck
        run: sudo luarocks install luacheck 1.2.0-1
      - name: Luacheck
        run: luacheck .

  container:
    runs-on: ubuntu-latest
    name: "docker: ${{ matrix.component }}"
    strategy:
      fail-fast: false
      matrix:
        include:
          - component: server
            context: ./server
            title: snikketx-server
            description: SnikketX Prosody server image
            licenses: Apache-2.0
          - component: web-portal
            context: ./web-portal
            title: snikketx-web-portal
            description: SnikketX web portal
            licenses: AGPL-3.0-or-later
          - component: cert-manager
            context: ./cert-manager
            title: snikketx-cert-manager
            description: SnikketX Let's Encrypt helper
            licenses: Apache-2.0
          - component: updater
            context: ./updater
            title: snikketx-updater
            description: SnikketX container updater service
            licenses: Apache-2.0
          - component: backup
            context: ./backup
            title: snikketx-backup
            description: SnikketX backup service
            licenses: Apache-2.0
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: snikketx/${{ matrix.component }}
          tags: |
            type=raw,value=ci
          labels: |
            org.opencontainers.image.title=${{ matrix.title }}
            org.opencontainers.image.description=${{ matrix.description }}
            org.opencontainers.image.licenses=${{ matrix.licenses }}
            org.opencontainers.image.vendor=Sudo-Ivan
            org.opencontainers.image.source=https://github.com/Sudo-Ivan/snikketx

      - name: Build image
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: ${{ matrix.context }}
          load: true
          push: false
          tags: snikketx/${{ matrix.component }}:ci
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            BUILD_SERIES=ci
            BUILD_ID=${{ github.run_number }}
            VCS_REF=${{ github.sha }}
            BUILD_DATE=${{ github.event.head_commit.timestamp || github.event.repository.updated_at }}
          cache-from: type=gha,scope=ci-${{ matrix.component }}
          cache-to: type=gha,mode=max,scope=ci-${{ matrix.component }}

      - name: Record local image digest
        id: digest
        run: |
          set -euo pipefail
          DIGEST="$(docker image inspect "snikketx/${{ matrix.component }}:ci" --format '{{index .RepoDigests 0}}' 2>/dev/null || true)"
          if [ -z "$DIGEST" ] || [[ "$DIGEST" != *"@"* ]]; then
            DIGEST="snikketx/${{ matrix.component }}:ci@$(docker image inspect "snikketx/${{ matrix.component }}:ci" --format '{{.Id}}')"
          fi
          IMAGE_ID="$(docker image inspect "snikketx/${{ matrix.component }}:ci" --format '{{.Id}}')"
          echo "image_id=${IMAGE_ID}" >> "$GITHUB_OUTPUT"
          echo "ref=snikketx/${{ matrix.component }}:ci" >> "$GITHUB_OUTPUT"
          {
            echo "## ${{ matrix.component }} (CI build)"
            echo ""
            echo "- image id: \`${IMAGE_ID}\`"
            echo "- tag: \`snikketx/${{ matrix.component }}:ci\`"
          } >> "$GITHUB_STEP_SUMMARY"
          printf '%s\n' "${IMAGE_ID}" > "digest-${{ matrix.component }}.txt"

      - name: Upload image id artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: image-id-${{ matrix.component }}
          path: digest-${{ matrix.component }}.txt

      - name: Smoke test
        run: ./scripts/smoke-test.sh "${{ matrix.component }}"

      - name: Generate SBOM with Syft
        uses: anchore/sbom-action@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26 # v0.24.2
        with:
          image: snikketx/${{ matrix.component }}:ci
          format: spdx-json
          artifact-name: sbom-${{ matrix.component }}.spdx.json
          output-file: sbom-${{ matrix.component }}.spdx.json

      - name: Trivy vulnerability scan
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: snikketx/${{ matrix.component }}:ci
          format: sarif
          output: trivy-${{ matrix.component }}.sarif
          severity: CRITICAL,HIGH
          exit-code: "1"
          ignore-unfixed: true

      - name: Upload Trivy SARIF
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: trivy-ci-${{ matrix.component }}
          path: trivy-${{ matrix.component }}.sarif
          if-no-files-found: ignore

      - name: Upload Trivy results to GitHub Security
        if: always() && github.event_name != 'pull_request'
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4.38.0
        with:
          sarif_file: trivy-${{ matrix.component }}.sarif
          category: trivy-ci-${{ matrix.component }}
