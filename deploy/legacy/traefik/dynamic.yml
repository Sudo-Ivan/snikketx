# Generated by scripts/render-edge.sh for chat.localhost (dev)
# Do not edit by hand.

http:
  routers:
    snikket-prosody:
      rule: (Host(`chat.localhost`) || Host(`share.chat.localhost`) || Host(`groups.chat.localhost`)) && (PathPrefix(`/upload`) || PathPrefix(`/http-bind`) || PathPrefix(`/xmpp-websocket`) || PathPrefix(`/admin_api`) || PathPrefix(`/invites_api`) || PathPrefix(`/invites_bootstrap`) || PathPrefix(`/.well-known/host-meta`))
      entryPoints:
        - web
      priority: 50
      service: snikket-prosody

    snikket-web:
      rule: Host(`chat.localhost`) || Host(`share.chat.localhost`) || Host(`groups.chat.localhost`)
      entryPoints:
        - web
      priority: 10
      service: ravenguard

  services:
    ravenguard:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: http://ravenguard:8080

    snikket-prosody:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: http://snikket_server:5280
